# website

Landing site for the project. Built using [Maudit](https://maudit.org/).

## Develop

```sh
cargo install maudit-cli   # once
maudit dev                 # live-reloading dev server
maudit build       				 # static output → ./dist
```
